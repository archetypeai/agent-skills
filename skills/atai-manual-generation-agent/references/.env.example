# Both variables are required — there is no default endpoint.
# NO /v0.5 suffix: the runner mounts /agents and /v0.5/files itself.
# API keys are deployment-scoped: a key issued for one deployment 401s
# against another.
ATAI_API_KEY=<your API key>
ATAI_API_ENDPOINT=https://api.u1.archetypeai.app
