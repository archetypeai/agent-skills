name: Test atai-newton-omega-model-data-prep

on:
  pull_request:
    paths:
      - 'skills/atai-newton-omega-model-data-prep/**'
      - '.github/workflows/test-atai-newton-omega-model-data-prep.yml'
  push:
    branches: [main]
    paths:
      - 'skills/atai-newton-omega-model-data-prep/**'
      - '.github/workflows/test-atai-newton-omega-model-data-prep.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12']
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r skills/atai-newton-omega-model-data-prep/references/requirements.txt pytest

      - name: Run pytest
        run: pytest skills/atai-newton-omega-model-data-prep/tests/ -v
