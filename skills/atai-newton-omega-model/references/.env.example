# Copy to .env and fill in BOTH values — the scripts require the endpoint
# explicitly (no default). Auto-loaded via python-dotenv if installed.

ATAI_API_KEY=
ATAI_API_ENDPOINT=https://api.u1.archetypeai.app/v0.5
